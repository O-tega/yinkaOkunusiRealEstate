export type TProperty = {
  l_id: number;
  l_code: string;
  l_category: string;
  l_type: string;
  l_slider: number;
  l_title: string;
  l_description: string;
  l_location: string;
  l_features: string;
  l_img1: string; // Assuming "land" is a string representing an image URL or path
  l_amount: string;
  l_currency: string;
  l_duration: string;
  l_status: boolean;
  l_date: string; // If it's a date string, it can remain as a string type
  l_time: string;
};
